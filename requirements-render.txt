# ══════════════════════════════════════════════════════
# RENDER.COM PRODUCTION REQUIREMENTS — MINIMAL SAFE BUILD
#
# Removed from local requirements.txt:
#   ✗ easyocr          — pulls PyTorch 2GB+
#   ✗ sentence-transformers — pulls onnxruntime → GPU crash
#   ✗ torch / torchvision  — too large for free tier
#   ✗ onnxruntime          — crashes on Render CPU-only box
#   ✗ pdf2image            — requires poppler binary
#   ✗ pytesseract          — requires tesseract binary
#
# chromadb 0.4.24 — last version before posthog telemetry bug
# ══════════════════════════════════════════════════════

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.3.0

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0

# LLM — current active Groq models (April 2026)
groq==0.9.0

# Agent Orchestration
langgraph==0.1.5
langchain-core==0.2.3

# Vector Store — use 0.4.24 to avoid posthog telemetry crash
# chromadb >= 0.5.0 has a posthog bug: capture() takes 1 arg but 3 given
chromadb==0.4.24

# OCR — PDF text layer only (no binary dependencies on cloud)
pypdf==4.2.0
Pillow==10.3.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
aiofiles==23.2.1
tenacity==8.3.0
numpy==1.26.4

# Testing (also used in CI)
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-timeout==2.3.1
pytest-cov==5.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
