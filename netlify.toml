# NOTE: This project has moved from Netlify to Cloudflare Pages.
# Cloudflare Pages config is handled via:
#   - ui/public/_redirects  (SPA routing)
#   - Cloudflare Pages dashboard env vars
#
# This file is kept only as reference. Not used for deployment.

[build]
  base    = "ui"
  command = "npm run build"
  publish = "dist"
